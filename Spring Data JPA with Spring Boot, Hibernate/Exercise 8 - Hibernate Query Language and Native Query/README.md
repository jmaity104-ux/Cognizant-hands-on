# Exercise 8: Hibernate Query Language and Native Query

This exercise shows two custom query styles.

- HQL/JPQL works with entity names and fields: `Country` and `name`.
- Native SQL works directly with table and column names: `country` and `name`.

Both queries search countries by part of the country name.
