# Exercise 7: O/R Mapping

This exercise demonstrates object-relational mapping with two entities.

- `Country` is mapped to a database table.
- `Capital` is mapped to another database table.
- `@OneToOne` connects one country with one capital city.

The Java relationship becomes a database relationship through Hibernate.
